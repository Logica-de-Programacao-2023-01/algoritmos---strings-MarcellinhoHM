//Escreva um programa que receba uma string e converta todas as letras minúsculas para maiúsculas. Informe ao usuário o resultado.
package main

import (
	"fmt"
	"strings"
)

func main() {
	String := "w o o o o o"
	fmt.Println(String)
	fmt.Println(strings.ReplaceAll(String, " ", ""))
}
